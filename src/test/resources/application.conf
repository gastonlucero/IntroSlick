datahack {
  url = "jdbc:postgresql://localhost:5432/postgres?user=postgres"
  driver = org.postgresql.Driver
  connectionPool = disabled
  keepAliveConnection = true
  autoCommit = true
}

schema {
  nombre = "test"
  tablaDispositivo = "dispositivos"
  tablaMensaje = "mensajes"
}